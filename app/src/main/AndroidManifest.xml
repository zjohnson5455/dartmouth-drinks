<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apptime.alexw.dartmouthdrinks">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CreateAcctActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BeerActivity" />
        <activity android:name=".ResourcesActivity" />
        <activity android:name=".PongActivity"></activity>
        <activity android:name=".AddActivity">
            <intent-filter>
                <action android:name="ADD"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".CreateNextActivity">
            <intent-filter>
                <action android:name="NEXT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="SIGN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".BACInfo">
            <intent-filter>
                <action android:name="INFO"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".SettingsActivity"/>
        <activity android:name=".TimeDrinkActivity"/>
        <service android:name=".ForegroundService"/>
        <activity android:name=".WelcomeActivity"/>
        <activity android:name=".GeoFencey"/>
        <service android:name=".GeofenceTransitionService"/>
    </application>
</manifest>